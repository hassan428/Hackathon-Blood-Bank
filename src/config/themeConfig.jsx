import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#a80101",
      // main: "#FF5900",
    },
    secondary: {
      main: "#ffb9b7",
      // main: "#FAE7DD",
    },
  },
});

export default theme;
